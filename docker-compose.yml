version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - SPOXY_REDIS_URL=redis://redis:6379/0
      - SPOXY_PROXY_URL=${SPOXY_PROXY_URL:-}
    depends_on:
      - redis

  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
